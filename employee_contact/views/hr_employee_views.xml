<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_employee_tree_test_task" model="ir.ui.view">
            <field name="name">hr.employee.tree.test.task</field>
            <field name="model">hr.employee.test.task</field>
            <field name="arch" type="xml">
                <tree string="Employees">
                    <field name="name"/>
                    <field name="first_name"/>
                    <field name="last_name"/>
                    <field name="email"/>
                </tree>
            </field>
        </record>

        <record id="view_employee_form_test_task" model="ir.ui.view">
            <field name="name">hr.employee.form.test.task</field>
            <field name="model">hr.employee.test.task</field>
            <field name="arch" type="xml">
                <form string="Employee Test Task">
                    <field name="active" invisible="1"/>
                    <sheet>
                        <widget name="web_ribbon" title="Archived" bg_color="bg-danger" attrs="{'invisible': [('active', '=', True)]}"/>
                        <div class="oe_title">
                            <h1 class="d-flex flex-row">
                                <field name="name" placeholder="Employee's Name"  force_save="1"
                                    required="False" style="font-size: min(4vw, 2.6rem);"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="first_name"/>
                                <field name="last_name"/>
                                <field name="email" widget="email"/>
                            </group>
                            <group>
                                <field name="active"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" groups="base.group_user" options="{'post_refresh': 'recipients'}"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <record id="open_view_employee_list_my" model="ir.actions.act_window">
            <field name="name">Employees</field>
            <field name="res_model">hr.employee.test.task</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[]</field>
            <field name="context">{}</field>
            <field name="view_id" eval="False"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Add a new employee
                </p>
                <p>
                    With just a quick glance on the Odoo employee screen, you
                    can easily find all the information you need for each person;
                    contact data, job position, availability, etc.
                </p>
            </field>
        </record>

    </data>
</odoo>
